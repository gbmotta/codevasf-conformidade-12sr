# =============================================================================
# Dependências Python do Hugging Face Space (Gradio + ZeroGPU)
# =============================================================================
# Mais enxuto que requirements.txt (sem Streamlit).
# torch não é pinado: já vem na imagem ZeroGPU / Gradio.
# Copiado para requirements.txt do Space por scripts/deploy_hf_space.py
# =============================================================================
python-dotenv>=1.0.0
pyyaml>=6.0.1
requests>=2.31.0
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
pymupdf>=1.24.0
pytesseract>=0.3.10
pillow>=10.0.0
# EasyOCR não entra no Space (cold start / RAM). Use OCR_ENGINE=tesseract (padrão auto sem easyocr).
huggingface_hub>=0.26.0
gradio>=5.0.0
spaces>=0.30.0
transformers>=4.45.0
accelerate>=0.34.0
fpdf2>=2.7.0
scikit-learn>=1.3.0
joblib>=1.3.0
